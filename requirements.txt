# --- 核心 Web 框架 ---
Flask==3.0.2
Werkzeug==3.0.1
flask-cors
gunicorn

# --- AI 与视觉引擎 (最核心) ---
# 仅安装 headless 版防止云端环境报错
opencv-python-headless
# ultralytics 会自动安装必要的 torch(CPU版)
ultralytics

# --- 阿里云大模型 SDK ---
dashscope>=1.14.0

# --- 基础工具 ---
requests>=2.31.0
python-dotenv